# 教练排课查询性能优化方案

## 问题分析

当前教练排课查询存在以下性能问题：

1. **多次数据库查询**：
   - 查询排课记录
   - 查询教练信息
   - 查询时间段字典
   - 对每个排课循环查询考勤记录
   - 对每个学员循环查询学员信息

2. **N+1查询问题**：
   - 查询N个排课记录后，需要对每个排课执行额外的查询

3. **响应时间过长**：
   - 当前响应时间约3950ms（约4秒）

## 优化方案

### 1. 创建数据库视图

已创建两个视图：

#### 视图1: `view_badminton_coach_schedule`
包含排课、班级、教练和考勤统计信息

#### 视图2: `view_badminton_coach_schedule_students`
包含排课信息和学员列表（JSON格式），一次性获取所有数据

### 2. 执行脚本

```bash
cd backend/sql

# 创建教练排课学员视图
python create_coach_schedule_students_view.py
```

### 3. 修改后端代码

#### 3.1 添加CRUD类（crud.py）
```python
class CoachScheduleCRUD(CRUDBase[None, None, None]):
    """教练排课视图查询"""
    
    def __init__(self, auth: AuthSchema) -> None:
        super().__init__(model=None, auth=auth)
        self.table_name = "view_badminton_coach_schedule_students"
    
    async def list_crud(self, search: Optional[dict] = None, order_by: Optional[list[dict]] = None) -> Sequence:
        """从视图中查询排课列表"""
        async with self.db.session_maker() as session:
            query = f"SELECT * FROM {self.table_name} WHERE 1=1"
            params = []
            
            # 添加搜索条件
            if search:
                for key, (operator, value) in search.items():
                    if value is not None:
                        if operator == "eq":
                            query += f" AND {key} = ${len(params) + 1}"
                            params.append(value)
            
            # 添加排序条件
            if order_by:
                order_clauses = []
                for order in order_by:
                    for key, direction in order.items():
                        order_clauses.append(f"{key} {direction.upper()}")
                query += f" ORDER BY {', '.join(order_clauses)}"
            
            result = await session.execute(query, params)
            rows = result.fetchall()
            columns = result.keys()
            return [dict(zip(columns, row)) for row in rows]
```

#### 3.2 修改Service方法（service.py）
```python
@staticmethod
async def get_coach_daily_schedule_service(
    auth: AuthSchema,
    redis: Redis,
    coach_id: int,
    schedule_date: str
) -> dict:
    """
    获取教练在指定日期的排课列表（按时间段分组）
    使用视图优化查询性能
    """
    # 1. 将字符串日期转换为 date 对象
    schedule_date_obj = datetime.strptime(schedule_date, "%Y-%m-%d").date()
    
    # 2. 使用视图查询教练在该日期的所有排课记录（包含学员列表）
    search = {
        "coach_id": ("eq", coach_id),
        "schedule_date": ("eq", schedule_date_obj)
    }
    order_by = [{"start_time": "asc"}]
    
    # 使用新的CRUD查询视图
    schedules = await CoachScheduleCRUD(auth).list_crud(search=search, order_by=order_by)
    
    if not schedules:
        return {
            "date": schedule_date,
            "coach_id": coach_id,
            "coach_name": "",
            "time_slots": []
        }
    
    # 3. 获取教练名称（从第一条记录中获取）
    coach_name = schedules[0].get("coach_name", "")
    
    # 4. 获取时间段字典数据
    time_slot_dict_list = await get_time_slot_dict_with_cache(redis)
    time_slot_dict = {}
    for slot in time_slot_dict_list:
        if isinstance(slot, dict) and 'dict_value' in slot:
            dict_label = slot.get('dict_label', '')
            if '-' in dict_label:
                time_parts = dict_label.split('-')
                start_time = time_parts[0].strip()
                end_time = time_parts[1].strip()
            else:
                start_time = '08:00'
                end_time = '09:30'
            time_slot_dict[slot['dict_value']] = {
                "start": start_time,
                "end": end_time,
                "dict_label": dict_label
            }
    
    # 5. 按时间段分组
    time_slot_groups = {}
    
    for schedule in schedules:
        time_slot_code = schedule.get("time_slot_code")
        if not time_slot_code:
            continue
        
        if time_slot_code not in time_slot_groups:
            slot_info = time_slot_dict.get(time_slot_code, {})
            time_slot_groups[time_slot_code] = {
                "time_slot_code": time_slot_code,
                "time_slot_name": f"{time_slot_code}时段",
                "start_time": slot_info.get("start", ""),
                "end_time": slot_info.get("end", ""),
                "schedules": []
            }
        
        # 6. 从视图中获取学员列表（已经是 JSON 格式）
        students = schedule.get("students_json", [])
        if isinstance(students, str):
            try:
                students = json.loads(students)
            except:
                students = []
        
        time_slot_groups[time_slot_code]["schedules"].append({
            "id": schedule.get("id"),
            "class_id": schedule.get("class_id"),
            "class_name": schedule.get("class_name", ""),
            "location": schedule.get("location"),
            "topic": schedule.get("topic"),
            "content_summary": schedule.get("content_summary"),
            "schedule_status": schedule.get("schedule_status"),
            "students": students,
            "student_count": schedule.get("student_count", len(students)),
            "attendance_count": schedule.get("attendance_count", len([s for s in students if s.get("has_attended", False)])),
            "notes": schedule.get("notes")
        })
    
    # 7. 按时间段代码排序
    time_slot_order = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J', 'K', 'L']
    sorted_time_slots = []
    
    for slot_code in time_slot_order:
        if slot_code in time_slot_groups:
            sorted_time_slots.append(time_slot_groups[slot_code])
    
    # 添加未在预定义顺序中的时间段
    for slot_code, group in time_slot_groups.items():
        if slot_code not in time_slot_order:
            sorted_time_slots.append(group)
    
    return {
        "date": schedule_date,
        "coach_id": coach_id,
        "coach_name": coach_name,
        "time_slots": sorted_time_slots
    }
```

## 优化效果

### 优化前
- 查询次数：1 + 1 + 1 + N + M（N=排课数，M=学员数）
- 响应时间：~3950ms（4秒）

### 优化后
- 查询次数：2（视图查询 + 时间段字典）
- 响应时间：预计 <200ms

**性能提升：约20倍**

## 实施步骤

1. 执行视图创建脚本
   ```bash
   cd backend/sql
   python create_coach_schedule_students_view.py
   ```

2. 修改后端代码（crud.py 和 service.py）

3. 重启后端服务

4. 测试验证性能提升